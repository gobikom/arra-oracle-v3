## Review Fix Summary

Applied fixes from review report: `pr-context-feat-issue-6-oracle-family-normalization.md`
(Issue findings sourced from agents-review output — codex-0280d8522973)

### Fixed Issues

| Severity | Fixed | Skipped | Already Fixed | Total |
|----------|-------|---------|---------------|-------|
| Critical | 0 | 0 | 0 | 0 |
| High (Important) | 4 | 0 | 0 | 4 |
| Medium | 0 | 0 | 0 | 0 |
| Suggestion | 0 | 7 | 0 | 7 |
| **Total** | **4** | **7** | **0** | **11** |

> Suggestions were not detailed in the review artifact and were skipped.

### Changes Made

| File | Fix |
|------|-----|
| `src/config.ts` | Upgraded HTTP+OAuth URL from warn to error using `resolveMcpExternalUrl()` helper with loopback detection |
| `src/oauth/provider.ts` | `saveState()` now throws on failure; critical callers (registerClient, token exchange) roll back in-memory state and surface error to client |
| `src/oauth/routes.ts` | Removed spoofable IP-based rate limiting; provider now uses global lockout window not bypassable via header forgery |
| `src/oauth/types.ts` | Added `failed_attempts: number` to `PendingAuthorization` (linter extension) |
| `src/integration/knowledge.test.ts` | New: isolated integration test for POST /api/learn persistence and searchability |
| `src/integration/oauth-hardening.test.ts` | New: integration tests for HTTP+OAuth rejection and global PIN lockout bypass test |
| `src/integration/http.test.ts` | Added learn route persistence + validation tests to existing HTTP integration suite |

### Commits

- `c05dd60` — `fix: harden oauth flow and learn route coverage` (linter)
- `f69a5e6` — `test: add POST /api/learn integration tests to http suite`

### Validation

| Check | Status |
|-------|--------|
| Type Check (`bun run build`) | ✅ No new errors |
| Lint | N/A |
| Tests (`bun run test:unit`) | ✅ 157 pass, 0 fail |
| Build | ✅ Pre-existing errors only (server-legacy.ts, handlers.ts — confirmed on main) |

### Pattern Expansions

| Pattern | File | Siblings | Action |
|---------|------|----------|--------|
| HTTP+OAuth URL validation | src/config.ts | 0 | Not pattern-class — single config check |
| saveState error propagation | src/oauth/provider.ts | 1 (cleanExpiredTokens — non-critical) | Non-critical callers kept as-is by design |
| IP header spoofing | src/oauth/routes.ts | 0 | Linter adopted global lockout — no siblings |
| Missing persistence test | src/integration/ | 0 | New files created |

---
*Automated review fixes — PR #715*
*Commits: c05dd60, f69a5e6*
